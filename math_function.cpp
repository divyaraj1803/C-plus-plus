#include <iostream>
#include <cmath>

int main(){
  double x = 2.49;
  double y = 1.95;
  double z;
  // z = std::max(x,y);
  // z = std::min(x,y);
  // z = pwr(2,4);
  // z = sqrt(16);
  // z = round(x);
  // z = ceil(x);
  // z = floor(x);

  std::cout << z;
}